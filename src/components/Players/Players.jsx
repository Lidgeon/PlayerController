import PlayersList from "./PlayersList/PlayersList";

import './Players.scss'

const Players = () => {
  return (
    <div className="players">
      <PlayersList />
    </div>
  );
};

export default Players;
